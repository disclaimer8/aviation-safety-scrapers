{
  "name": "ntsb-ingest",
  "version": "0.1.0",
  "description": "Build a standalone SQLite of US NTSB aviation accident reports from the public avall.zip bulk dump",
  "license": "Apache-2.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node src/cli.js build",
    "selftest": "node src/cli.js --selftest",
    "test": "jest"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0"
  },
  "devDependencies": {
    "jest": "^29.7.0"
  },
  "overrides": {
    "js-yaml": "^4.2.0"
  }
}
