{
  "name": "atsb-ingest",
  "version": "0.1.0",
  "description": "Ingest ATSB (Australian Transport Safety Bureau) aviation investigations into a standalone SQLite via a headed Playwright browser",
  "license": "Apache-2.0",
  "private": true,
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "build": "node src/cli.js build",
    "test": "jest"
  },
  "dependencies": {
    "better-sqlite3": "^12.10.0",
    "cheerio": "^1.0.0"
  },
  "peerDependencies": {
    "playwright": "^1.40.0"
  },
  "peerDependenciesMeta": {
    "playwright": {
      "optional": true
    }
  },
  "devDependencies": {
    "jest": "^29.7.0"
  }
}
