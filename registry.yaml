# GENERATED by scripts/build_registry.py - do not edit by hand.
# Run `python scripts/build_registry.py` after adding or changing a source.
# python_sources: 121
# four_verb_packages: 75
# single_file_scripts: 46
# with_behaviour_tests: 76
# import_smoke_only: 45
# no_tests: 0
sources:
  - code: aacm
    shape: script
    verbs: [argv]
    tests: smoke
    country: MO
    delay_seconds: 3.0
    summary: 'aacm — Civil Aviation Authority of Macao SAR aviation accident/incident ingest.'
  - code: aacsv
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 500
    summary: 'discover -> fetch -> parse -> build pipeline for El Salvador AAC.'
  - code: aaiahk
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for HK AAIA (single register page).'
  - code: aaiasb
    shape: package
    verbs: [# aaiasb.eu pipeline (unchanged)
            "discover, fetch, parse, build, all, # harsia.gr pipeline (post-2023, CF-protected)
            "discover-harsia, fetch-harsia, parse-harsia, build-harsia, all-harsia, ]
    tests: behaviour
    delay_seconds: 1.5
    narrative_floor: 80
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for AAIASB (Greece).'
  - code: aaib
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: GB
    delay_seconds: 1.0
    pdf_text_min: 600
    user_agent: 'aaib-ingest/1.0'
  - code: aaibly
    shape: script
    verbs: [argv]
    tests: smoke
    country: LY
    summary: 'aaibly — Libya Air Accidents Investigation Bureau (5A/Libya)'
  - code: aaibmm
    shape: script
    verbs: [argv]
    tests: smoke
    country: MM
    delay_seconds: 2.0
    summary: 'aaibmm — Myanmar Aircraft Accident Investigation Bureau (AAIB / DCA Myanmar) ingest.'
  - code: aaibmn
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.8
    narrative_floor: 30
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for Mongolia AAIB.'
  - code: aaibmy
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: MY
    delay_seconds: 1.5
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch(+parse) → build pipeline for AAIB Malaysia.'
  - code: aaibzm
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for AAIB Zambia (single index page).'
  - code: aaicmv
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for AAIC Maldives.'
  - code: aaicnp
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for Nepal AAIC (CAAN).'
  - code: aaicth
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    summary: 'discover → fetch → parse → build pipeline for Thailand AAIC.'
  - code: aaid
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for Kenya AAID /final-reports.'
  - code: aaig
    shape: script
    verbs: [argv]
    tests: smoke
    country: BD
    delay_seconds: 2.0
    summary: 'AAIG (Bangladesh — Aircraft Accident Investigation Group, under CAAB) ingest.'
  - code: aaiib
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for AAIIB (Philippines).'
  - code: aaiibcy
    shape: script
    verbs: [argv]
    tests: smoke
    country: CY
    delay_seconds: 1.5
    pdf_text_min: 300
    summary: 'aaiibcy — Cyprus AAIIB (Aircraft Accident and Incident Investigation Board)'
  - code: aaikz
    shape: script
    verbs: [argv]
    tests: smoke
    country: KZ
    summary: "aaikz (Kazakhstan – Министерство транспорта РК / ДРПИТ, country KZ, lang 'ru')"
  - code: aaisl
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for AAII Sri Lanka (CAA Sri Lanka).'
  - code: aaiu
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: IE
    delay_seconds: 1.5
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for AAIU Ireland.'
  - code: aaiube
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: BE
    delay_seconds: 1.5
    narrative_floor: 300
    summary: 'discover → fetch → build pipeline for AAIU Belgium.'
  - code: aaiubg
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for Bulgaria AAIU (source key aaiubg).'
  - code: aaiuz
    shape: script
    verbs: [argv]
    tests: smoke
    country: UZ
    delay_seconds: 2.0
    pdf_text_min: 300
    user_agent: 'Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36'
    summary: 'Patchright helper: render mintrans.uz listing page and print HTML to stdout.'
  - code: aet
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for Luxembourg AET (single page).'
  - code: agacgn
    shape: script
    verbs: [argv]
    tests: smoke
    country: GN
    delay_seconds: 2.0
    summary: "agacgn — AGAC Guinea (Autorité Guinéenne de l'Aviation Civile, GN)"
  - code: ahac
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: HN
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for Honduras AHAC.'
  - code: aias
    shape: script
    verbs: [argv]
    tests: smoke
    country: RO
    delay_seconds: 2.0
    pdf_text_min: 600
    summary: 'AIAS (Romania) accident/incident report ingest — patchright CF transport.'
  - code: aibdk
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: DK
    delay_seconds: 2.0
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for AIB Denmark.'
  - code: aibsa
    shape: script
    verbs: [argv]
    tests: smoke
    country: SA
    delay_seconds: 2.0
    summary: 'aibsa — Saudi Arabia Aviation Investigation Bureau (AIB) ingest.'
  - code: aicpng
    shape: package
    verbs: [discover, refetch, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for AIC PNG.'
  - code: ainhr
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for AIN.HR (Croatia) aviation reports.'
  - code: akisa
    shape: script
    verbs: [argv]
    tests: smoke
    country: AL
    delay_seconds: 2.0
    pdf_text_min: 600
    summary: 'AKISA (Albania — Autoriteti Kombëtar i Investigimit për Sigurinë e Operimit'
  - code: anacdz
    shape: script
    verbs: [argv]
    tests: smoke
    country: DZ
    delay_seconds: 2.0
    summary: "anacdz — ANAC Algeria (Agence Nationale de l'Aviation Civile, DZ)"
  - code: ansv
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: IT
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for ANSV (Italy) WordPress listing.'
  - code: araib
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: KR
    delay_seconds: 1.0
    narrative_floor: 300
    summary: 'discover → fetch(+detail+parse) → build pipeline for ARAIB South Korea.'
  - code: asrs
    shape: script
    verbs: [argv]
    tests: smoke
    country: US
    summary: 'ASRS NASA Voluntary Incident Reports Harvester'
  - code: baaid
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for Bahamas AAID (baaid.org).'
  - code: bagaia
    shape: package
    verbs: [seed, discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'seed → fetch → parse → build pipeline for BAGAIA.'
  - code: bdca
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for Belize DCA / BAAI.'
  - code: bea
    shape: package
    verbs: [discover, refetch, fetch, parse, build, all]
    tests: behaviour
    country: FR
    delay_seconds: 1.5
    narrative_floor: 300
    pdf_text_min: 600
    user_agent: 'bea-ingest/1.0'
    summary: 'discover → fetch → parse → build pipeline for bea.aero notified-events.'
  - code: beabj
    shape: script
    verbs: [argv]
    tests: smoke
    country: BJ
    delay_seconds: 2.0
    summary: 'beabj — Bureau Enquêtes-Accidents du Bénin (BEA-Bénin)'
  - code: beacg
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for BEA Congo (single index page).'
  - code: beaci
    shape: script
    verbs: [argv]
    tests: smoke
    country: CI
    delay_seconds: 2.0
    summary: "beaci — Bureau d'Enquêtes Accidents d'Aviation de Côte d'Ivoire (BEA-CI)"
  - code: beam
    shape: script
    verbs: [argv]
    tests: smoke
    country: MA
    delay_seconds: 2.0
    summary: "beam (Morocco BEAM — Bureau d'Enquêtes et d'Analyses, country MA, lang 'fr')"
  - code: beasn
    shape: script
    verbs: [argv]
    tests: smoke
    country: SN
    delay_seconds: 2.0
    summary: 'beasn — Senegal BEA aviation-accident ingest.'
  - code: bfu
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: DE
    delay_seconds: 3.0
    narrative_floor: 300
    pdf_text_min: 600
    user_agent: 'bfu-ingest/1.0'
    summary: 'discover → fetch → parse → build pipeline for BFU Untersuchungsberichte.'
  - code: bhdca
    shape: script
    verbs: [argv]
    tests: smoke
    country: BA
    summary: 'bhdca — Bosnia and Herzegovina Directorate of Civil Aviation'
  - code: bpea
    shape: script
    verbs: [argv]
    tests: smoke
    country: CD
    delay_seconds: 3.0
    summary: 'bpea — Democratic Republic of Congo BPEA aviation-accident ingest.'
  - code: caav
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for CAAV (Vietnam).'
  - code: caavvu
    shape: script
    verbs: [argv]
    tests: smoke
    country: VU
    delay_seconds: 2.0
    summary: 'caavvu — Civil Aviation Authority of Vanuatu (CAAV) accident investigation ingest.'
  - code: caoiran
    shape: script
    verbs: [argv]
    tests: smoke
    country: IR
    delay_seconds: 3.0
    narrative_floor: 300
    summary: 'caoiri — Iran CAO Aircraft Accident Investigation Board (AAIB)'
  - code: cenipa
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: BR
    delay_seconds: 3.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for CENIPA (Brazil).'
  - code: ciaado
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: DO
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for CIAA (Dominican Republic).'
  - code: ciaape
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: PE
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for CIAA Peru (gob.pe, multi-hop).'
  - code: ciaiac
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: ES
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for CIAIAC (Spain) per-year listing.'
  - code: ciaiauy
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: UY
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for CIAIA (Uruguay).'
  - code: cins
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: RS
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for CINS (Serbia).'
  - code: cipaa
    shape: script
    verbs: [argv]
    tests: smoke
    country: PY
    summary: 'cipaa (Paraguay – Centro de Investigación y Prevención de Accidentes Aeronáuticos,'
  - code: daaina
    shape: script
    verbs: [argv]
    tests: smoke
    country: NA
    delay_seconds: 2.5
    summary: 'daaina (Namibia DAAI — Directorate of Aircraft Accident Investigations, country NA)'
  - code: daibw
    shape: script
    verbs: [argv]
    tests: smoke
    country: BW
    delay_seconds: 2.0
    summary: 'daibw — Botswana Directorate of Accident Investigation (DAI) ingest.'
  - code: dgacbo
    shape: script
    verbs: [argv]
    tests: smoke
    country: BO
    delay_seconds: 1.5
    summary: 'DGACBO (Bolivia — Dirección General de Aeronáutica Civil, AIG) aviation-accident ingest.'
  - code: dgaccl
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: CL
    delay_seconds: 1.5
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for DGAC Chile.'
  - code: dgaccr
    shape: script
    verbs: [argv]
    tests: smoke
    country: CR
    delay_seconds: 2.0
    summary: "dgaccr (Costa Rica DGAC, country CR, lang 'es') aviation-accident ingest."
  - code: dgacec
    shape: script
    verbs: [argv]
    tests: smoke
    country: EC
    delay_seconds: 2.0
    summary: 'DGACEC (Ecuador — Dirección General de Aviación Civil) aviation-accident ingest.'
  - code: dgacgt
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: GT
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for DGAC Guatemala (UIA).'
  - code: dgcakw
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 300
    user_agent: 'Mozilla/5.0 (X11; Linux x86_64) AppleWebKit/537.36'
    summary: 'discover → fetch → parse → build pipeline for Kuwait DGCA (dgcakw).'
  - code: dgcalb
    shape: script
    verbs: [argv]
    tests: smoke
    country: LB
    summary: "dgcalb (Lebanon DGCA — Directorate General of Civil Aviation, country LB, lang 'en')"
  - code: eaaid
    shape: package
    verbs: [discover, fetch, parse, build, all, status]
    tests: behaviour
    delay_seconds: 2.0
    pdf_text_min: 300
    summary: 'discover → fetch → parse → build pipeline for EAAID (Egypt).'
  - code: eaibet
    shape: script
    verbs: [argv]
    tests: smoke
    country: ET
    summary: 'eaibet — Ethiopian Aircraft Accident Investigation Bureau (ET/Ethiopia)'
  - code: eccaa
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for ECCAA (Eastern Caribbean CAA).'
  - code: gcaa
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: AE
    delay_seconds: 1.5
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for GCAA UAE.'
  - code: gcaagy
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for GCAA Guyana (single AAID page).'
  - code: ghana
    shape: script
    verbs: [argv]
    tests: behaviour
    country: GH
    delay_seconds: 1.5
    user_agent: 'ghana-scraper/1.0 (+https://github.com/denyskolomiiets/aviation-safety-scrapers)'
    summary: 'Ghana AIB (Aircraft Accident & Incident Investigation & Prevention Bureau)'
  - code: gpiaaf
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: PT
    delay_seconds: 1.5
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → build pipeline for GPIAAF Portugal (civil aviation).'
  - code: griaa
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: CO
    delay_seconds: 2.0
    narrative_floor: 300
    summary: 'discover → fetch → parse → build pipeline for GRIAA (Colombia) per-year listing.'
  - code: iacm
    shape: script
    verbs: [argv]
    tests: smoke
    country: MZ
    delay_seconds: 2.0
    pdf_text_min: 600
    summary: 'IACM (Mozambique civil aviation authority) accident/incident report ingest.'
  - code: india
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: IN
    delay_seconds: 2.0
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for AAIB India.'
  - code: ipiaam
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for IPIAAM Cabo Verde.'
  - code: israel
    shape: script
    verbs: [argv]
    tests: smoke
    country: IL
    delay_seconds: 2.5
    pdf_text_min: 600
  - code: jcaa
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.5
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for JCAA Jamaica.'
  - code: jiaacve
    shape: package
    verbs: [discover, fetch, parse, build, all, status]
    tests: behaviour
    delay_seconds: 1.5
    narrative_floor: 300
    summary: 'discover → fetch → parse → build pipeline for JIAAC Venezuela.'
  - code: jst
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: AR
    delay_seconds: 1.5
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for JST Argentina.'
  - code: jtsb
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: JP
    delay_seconds: 1.5
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for JTSB (Japan) listing.'
  - code: kbsz
    shape: script
    verbs: [argv]
    tests: smoke
    country: HU
    delay_seconds: 2.0
    summary: 'Batch download remaining HU PDFs: generate curl script on hetzner, rsync back.'
  - code: kinsiv
    shape: script
    verbs: [argv]
    tests: smoke
    country: MK
    delay_seconds: 3.0
    narrative_floor: 300
    summary: 'kinsiv (North Macedonia КИНСИВ — Комитет за истрага на воздухопловни несреќи и сериозни инциденти)'
  - code: knkt
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: ID
    delay_seconds: 1.5
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for KNKT / NTSC Indonesia.'
  - code: malta
    shape: script
    verbs: [argv]
    tests: smoke
    country: MT
    delay_seconds: 2.5
    summary: 'Malta BAAI (Bureau of Air Accident Investigation) ingest — patchright CF.'
  - code: mexico
    shape: script
    verbs: [argv]
    tests: smoke
    country: MX
    delay_seconds: 2.5
    pdf_text_min: 600
    summary: "Mexico AFAC (Agencia Federal de Aviacion Civil) 'Informes Finales' ingest --"
  - code: mzisi
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for MzI (Slovenia) aviation reports.'
  - code: nbaai
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.8
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for NBAAI (Ukraine).'
  - code: nsia
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: NO
    delay_seconds: 1.5
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for NSIA Norway.'
  - code: nsib
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: NG
    delay_seconds: 1.5
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover -> fetch -> parse -> build pipeline for NSIB (Nigeria).'
  - code: ntsbaar
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: US
    delay_seconds: 1.5
    narrative_floor: 1000
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build for the NTSB Aircraft Accident Reports.'
  - code: ntsbcarol
    shape: package
    verbs: [discover, fetch, parse, build, all, count, verify]
    tests: behaviour
    country: US
    delay_seconds: 1.5
    narrative_floor: 200
    pdf_text_min: 500
    summary: 'discover → fetch → parse → build pipeline for ntsbcarol.'
  - code: ojk
    shape: script
    verbs: [argv]
    tests: smoke
    country: EE
    delay_seconds: 1.5
    pdf_text_min: 600
    summary: 'OJK / ESIB (Estonia) aviation accident-report ingest — plain httpx.'
  - code: otkes
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: FI
    delay_seconds: 1.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → build pipeline for OTKES Finland.'
  - code: otsb
    shape: script
    verbs: [argv]
    tests: smoke
    country: OM
    delay_seconds: 2.0
    summary: 'otsb (Oman — Oman Transport Safety Bureau, MTCIT) aviation-accident ingest.'
  - code: ovv
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: NL
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch(+parse) → build pipeline for OVV / Dutch Safety Board.'
  - code: panama
    shape: script
    verbs: [argv]
    tests: smoke
    country: PA
    delay_seconds: 1.5
    pdf_text_min: 600
    summary: 'Panama AAC / UPIA accident-report ingest — plain httpx (no browser).'
  - code: pkbwl
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: PL
    delay_seconds: 1.2
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for PKBWL Poland.'
  - code: rnsa
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: IS
    delay_seconds: 1.0
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for RNSA Iceland (aviation).'
  - code: rosap
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    country: US
    delay_seconds: 2.5
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build for the ROSA P CAB collection.'
  - code: sacaa
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: ZA
    delay_seconds: 1.5
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch(+parse) → build pipeline for SACAA AIID (South Africa).'
  - code: scaa
    shape: script
    verbs: [argv]
    tests: smoke
    country: SC
    delay_seconds: 1.5
    pdf_text_min: 600
    user_agent: 'curl/7.88.1'
    summary: 'SCAA (Seychelles Civil Aviation Authority, scaa.sc) accident-report ingest.'
  - code: shk
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: SE
    delay_seconds: 1.5
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for SHK Sweden.'
  - code: sibpk
    shape: script
    verbs: [argv]
    tests: smoke
    country: PK
    delay_seconds: 2.0
    summary: 'sibpk — Pakistan SIB/BASI/AAIB aviation-accident ingest.'
  - code: sub
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: AT
    delay_seconds: 1.0
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for SUB Austria.'
  - code: sust
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: CH
    delay_seconds: 1.75
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for SUST / STSB Switzerland.'
  - code: svk
    shape: script
    verbs: [argv]
    tests: smoke
    country: SK
    delay_seconds: 1.5
    pdf_text_min: 600
    summary: 'SVK (Slovakia) accident/incident final-report ingest — plain httpx, no browser.'
  - code: taibtz
    shape: script
    verbs: [argv]
    tests: smoke
    country: TZ
    delay_seconds: 2.0
    summary: 'taibtz — Tanzania Aircraft Accident Investigation Bureau (AAIB Tanzania / TAIB) ingest.'
  - code: taic
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: NZ
    delay_seconds: 2.0
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for TAIC (New Zealand).'
  - code: taiib
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 2.0
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for TAIIB (Latvia).'
  - code: taits
    shape: script
    verbs: [argv]
    tests: smoke
    country: LT
    narrative_floor: 80
    summary: 'Salvage builder: populate taits_accidents from detail-page summary_text.'
  - code: tsb
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: CA
    delay_seconds: 1.0
    narrative_floor: 300
    summary: 'discover → fetch+parse → build pipeline for TSB (Transportation Safety Board of Canada).'
  - code: tsib
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: SG
    delay_seconds: 1.5
    narrative_floor: 300
    summary: 'discover -> fetch(+parse) -> build pipeline for TSIB Singapore.'
  - code: ttcaa
    shape: package
    verbs: [discover, fetch, parse, build, all]
    tests: behaviour
    delay_seconds: 1.5
    narrative_floor: 300
    pdf_text_min: 600
    summary: 'discover → fetch → parse → build pipeline for TTCAA (Trinidad & Tobago).'
  - code: ttsb
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: TW
    delay_seconds: 1.0
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for TTSB Taiwan.'
  - code: ueim
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: TR
    delay_seconds: 1.0
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for UEIM Turkey.'
  - code: ukmaa
    shape: script
    verbs: [argv]
    tests: smoke
    country: GB
    delay_seconds: 1.5
    pdf_text_min: 300
    summary: 'OCR recovery script for ukmaa stubs (narrative < 300 chars).'
  - code: usafaib
    shape: script
    verbs: [argv]
    tests: smoke
    country: US
    delay_seconds: 2.0
    pdf_text_min: 600
    summary: 'USAF Accident Investigation Board (AIB) reports ingest — httpx, no browser needed.'
  - code: uzpln
    shape: package
    verbs: [discover, fetch, build, all]
    tests: behaviour
    country: CZ
    delay_seconds: 1.0
    narrative_floor: 300
    summary: 'discover → fetch(+parse) → build pipeline for UZPLN (Czech Republic).'
