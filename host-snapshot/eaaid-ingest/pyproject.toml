[project]
name = "eaaid-ingest"
version = "0.1.0"
requires-python = ">=3.9"
description = "Egypt EAAID aviation accident ingest for FlightFinder"
dependencies = []

[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.backends.legacy:build"

[tool.setuptools.packages.find]
where = ["."]
include = ["eaaid_ingest*"]
