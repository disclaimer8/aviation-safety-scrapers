[project]
name = "nbaai-ingest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.setuptools.packages.find]
include = ["nbaai_ingest*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
