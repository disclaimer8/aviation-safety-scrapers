[project]
name = "ciaiauy-ingest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["httpx>=0.28.1"]

[project.optional-dependencies]
dev = ["pytest>=9.1.1"]

[tool.setuptools.packages.find]
include = ["ciaiauy_ingest*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
