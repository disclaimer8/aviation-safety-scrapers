[project]
name = "uzpln-ingest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["httpx[socks]>=0.27", "certifi"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.setuptools.packages.find]
include = ["uzpln_ingest*"]

[tool.pytest.ini_options]
testpaths = ["tests"]
