[project]
name = "mexico-ingest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["patchright>=1.62.3"]

[project.optional-dependencies]
dev = ["pytest>=9.1.1"]

[tool.setuptools]
py-modules = ["mexico_scraper"]

[tool.pytest.ini_options]
testpaths = ["tests"]
