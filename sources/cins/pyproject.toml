[project]
name = "cins-ingest"
version = "0.1.0"
requires-python = ">=3.11"

dependencies = ["httpx[socks]>=0.28.1"]

[project.optional-dependencies]
dev = ["pytest>=9.1.1"]

[tool.setuptools]
py-modules = ["cins_ocr_backfill"]

[tool.pytest.ini_options]
testpaths = ["tests"]
