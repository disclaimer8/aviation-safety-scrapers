[project]
name = "daaina-ingest"
version = "0.1.0"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.setuptools]
py-modules = ["daaina_scraper"]

[tool.pytest.ini_options]
testpaths = ["tests"]
