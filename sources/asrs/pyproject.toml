[project]
name = "asrs-ingest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["requests>=2.31"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.setuptools]
py-modules = ["harvest"]

[tool.pytest.ini_options]
testpaths = ["tests"]
