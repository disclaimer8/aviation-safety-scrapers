[project]
name = "akisa-ingest"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = ["httpx>=0.27"]

[project.optional-dependencies]
dev = ["pytest>=8"]

[tool.setuptools]
py-modules = ["akisa_scraper"]

[tool.pytest.ini_options]
testpaths = ["tests"]
