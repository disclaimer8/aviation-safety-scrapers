[project]
name = "taits-ingest"
version = "0.1.0"
requires-python = ">=3.11"

[project.optional-dependencies]
dev = ["pytest>=9.1.1"]

[tool.setuptools]
py-modules = ["taits_build_summary", "taits_rebuild_pdf"]

[tool.pytest.ini_options]
testpaths = ["tests"]
